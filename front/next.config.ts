import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // ポート設定は環境変数 PORT または --port オプションで指定可能
  // 例: PORT=3001 yarn dev または yarn dev:3001
};

export default nextConfig;
